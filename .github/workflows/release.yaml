name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

env:
  KAWARIMI_LINUX_CI: "1"

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    container:
      image: swift:6.2-noble
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Trust workspace for SwiftPM
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Generate build info
        run: |
          set -euo pipefail
          VERSION="$(git describe --tags --always)"
          printf '%s\n' \
            '// Generated by release workflow — do not edit.' \
            'enum BuildInfo {' \
            "    static let version = \"$VERSION\"" \
            '}' \
            > Sources/Kawarimi/Generated.swift

      - name: Verify version matches tag
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF#refs/tags/}"
          VERSION="$(sed -n 's/.*static let version = "\(.*\)".*/\1/p' Sources/Kawarimi/Generated.swift)"
          if [ "$VERSION" != "$TAG" ]; then
            echo "Generated.swift version is '$VERSION', expected tag '$TAG'"
            exit 1
          fi
          echo "BuildInfo.version=$VERSION"

      - name: Create source archive
        run: |
          set -euo pipefail
          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            VERSION="${GITHUB_REF_NAME}"
          else
            VERSION="$(sed -n 's/.*static let version = "\(.*\)".*/\1/p' Sources/Kawarimi/Generated.swift)"
          fi
          ARCHIVE="kawarimi-${VERSION}-source.tar.gz"
          tar czf "$ARCHIVE" \
            --exclude='.git' \
            --exclude='.build' \
            --exclude='*.tar.gz' \
            .
          echo "ARCHIVE=$ARCHIVE" >> "$GITHUB_ENV"

      - name: Run tests
        run: swift test

      - name: Upload release asset
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ARCHIVE }}
          fail_on_unmatched_files: true
