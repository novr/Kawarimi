# swift-openapi-generator: generate は配列で指定する（Sequence）
# https://github.com/apple/swift-openapi-generator#configuration
generate:
  - types
  - client
  - server
# KawarimiPlugin の package ハンドラが Operations.* を参照するため、同じ access に揃える
accessModifier: package
