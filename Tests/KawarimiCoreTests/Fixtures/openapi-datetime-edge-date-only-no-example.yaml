openapi: 3.0.3
info:
  title: DateTimeEdgeDateOnlyNoExample
  version: "1"
paths:
  /d:
    get:
      operationId: getDateOnlyNoExample
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  day:
                    type: string
                    format: date
