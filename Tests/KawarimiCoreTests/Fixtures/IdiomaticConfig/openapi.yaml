openapi: 3.0.3
info:
  title: Idiomatic naming fixture
  version: 1.0.0
paths:
  /x:
    get:
      operationId: getGreeting
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
