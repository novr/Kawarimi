openapi: 3.0.3
info:
  title: DateTimeEdgeUnparseable
  version: "1"
paths:
  /bad:
    get:
      operationId: getDateTimeUnparseable
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  t:
                    type: string
                    format: date-time
                    example: "not-a-valid-instant"
